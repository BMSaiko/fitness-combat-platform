{
  "name": "@fitness-combat/web-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Web admin dev not configured'",
    "build": "echo 'Web admin build not configured'",
    "test": "echo 'Tests not configured' && exit 0",
    "lint": "eslint src/**/*.{ts,tsx} --ext .ts,.tsx",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^18.3.26",
    "@types/react-dom": "^18.3.7",
    "typescript": "^5.3.3"
  }
}
